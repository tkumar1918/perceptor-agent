# ============================================================================
# Perceptor VM agent — deploy on each PROJECT VM (one per host).
#
#   cp .env.example .env    # fill in EDGE_ENDPOINT, PROJECT_TOKEN, VM_NAME
#   docker compose up -d
#
# Ships this VM's host + container infra to the project's tenant, through the
# platform edge, tagged telemetry_source=infra. No inbound ports are opened.
# ============================================================================
name: perceptor-agent

services:
  agent:
    image: grafana/alloy:v1.17.1
    container_name: perceptor-agent
    restart: unless-stopped
    # Safety caps so the agent can never starve the app it shares the VM with,
    # even under a pathological scrape/log loop. Steady state is ~220MB / a few
    # %% CPU. On a small VM lower `cpus`; on a container-dense host raise
    # `mem_limit` (cadvisor memory scales with container count).
    mem_limit: 512m
    cpus: 2
    command:
      - run
      - /etc/alloy/config.alloy
      - --storage.path=/var/lib/alloy/data
      - --server.http.listen-addr=0.0.0.0:12345
    env_file: .env                         # EDGE_ENDPOINT, PROJECT_TOKEN, VM_NAME
    # Host + cgroup + containerd access for the node & cadvisor exporters.
    privileged: true
    pid: host
    cgroup: host
    volumes:
      - ./config.alloy:/etc/alloy/config.alloy:ro
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/host/root:ro,rslave
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /run/containerd/containerd.sock:/run/containerd/containerd.sock:ro
      # systemd collector (node exporter) talks to systemd over the host D-Bus
      # socket — without this mount it loads but reports no unit state.
      - /run/dbus/system_bus_socket:/run/dbus/system_bus_socket:ro
      - /var/lib/docker/:/var/lib/docker/:ro
      - /var/log/journal:/var/log/journal:ro
      - /etc/machine-id:/etc/machine-id:ro
      - agent-data:/var/lib/alloy/data
    logging:
      driver: json-file
      options: { max-size: "100m", max-file: "3" }

volumes:
  agent-data:
